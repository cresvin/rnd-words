/**
 * Generate random words
 * @param {number} numberOfWords - number of words to generate
 * @param {string} separator - separator between words
 * @param {boolean} randomNumbers - whether to include random numbers
 * @returns {string}
 */
export function generate(
  numberOfWords?: number,
  separator?: string,
  randomNumbers?: boolean
): string;
